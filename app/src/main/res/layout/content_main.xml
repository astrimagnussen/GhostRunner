<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="mycompany.ghostrunner.MainActivity"
    tools:showIn="@layout/activity_main">

    <Button android:id="@+id/coordinate_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Coordinate"
        android:onClick="coordinate"/>

    <Button android:id="@+id/saveLocBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/save_loc_btn"
        android:layout_below="@id/coordinate_button"
        android:onClick="saveLocation" />

    <Button android:id="@+id/calcDist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/calc_dist_btn"
        android:layout_below="@id/saveLocBtn"
        android:onClick="calcDistance" />

    <TextView android:id="@+id/startDistance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/calcDist" />

    <TextView android:id="@+id/showDistance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/startDistance"/>

</RelativeLayout>
